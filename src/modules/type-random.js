function text (array) {

	function number () { return Math.floor(Math.random() * array.length); }
	let bacate = [];

	let a = array[number()];
	let b = array[number()];
	let c = array[number()];
	let d = array[number()];
	let e = array[number()];
	let f = array[number()];
	let g = array[number()];
	let h = array[number()];
	let i = array[number()];
	let j = array[number()];
	let k = array[number()];
	let l = array[number()];
	let m = array[number()];
	let n = array[number()];
	let o = array[number()];
	let p = array[number()];
	let q = array[number()];
	let r = array[number()];
	let s = array[number()];
	let t = array[number()];


	if(bacate.includes(a)) {
		let fd = number();
		if(!bacate.includes(array[fd])) {
			bacate.push(array[fd]);
		}
	}
	else {
		bacate.push(a);
	}

	if(bacate.includes(b)) {
		let fd = number();
		if(!bacate.includes(array[fd])) {
			bacate.push(array[fd]);
		}
	}
	else {
		bacate.push(b);
	}

	if(bacate.includes(c)) {
		let fd = number();
		if(!bacate.includes(array[fd])) {
			bacate.push(array[fd]);
		}
	}
	else {
		bacate.push(c);
	}

	if(bacate.includes(d)) {
		let fd = number();
		if(!bacate.includes(array[fd])) {
			bacate.push(array[fd]);
		}
	}
	else {
		bacate.push(d);
	}

	if(bacate.includes(e)) {
		let fd = number();
		if(!bacate.includes(array[fd])) {
			bacate.push(array[fd]);
		}
	}
	else {
		bacate.push(e);
	}

	if(bacate.includes(f)) {
		let fd = number();
		if(!bacate.includes(array[fd])) {
			bacate.push(array[fd]);
		}
	}
	else {
		bacate.push(f);
	}

	if(bacate.includes(g)) {
		let fd = number();
		if(!bacate.includes(array[fd])) {
			bacate.push(array[fd]);
		}
	}
	else {
		bacate.push(g);
	}

	if(bacate.includes(h)) {
		let fd = number();
		if(!bacate.includes(array[fd])) {
			bacate.push(array[fd]);
		}
	}
	else {
		bacate.push(h);
	}

	if(bacate.includes(i)) {
		let fd = number();
		if(!bacate.includes(array[fd])) {
			bacate.push(array[fd]);
		}
	}
	else {
		bacate.push(i);
	}

	if(bacate.includes(j)) {
		let fd = number();
		if(!bacate.includes(array[fd])) {
			bacate.push(array[fd]);
		}
	}
	else {
		bacate.push(j);
	}

	if(bacate.includes(k)) {
		let fd = number();
		if(!bacate.includes(array[fd])) {
			bacate.push(array[fd]);
		}
	}
	else {
		bacate.push(k);
	}

	if(bacate.includes(l)) {
		let fd = number();
		if(!bacate.includes(array[fd])) {
			bacate.push(array[fd]);
		}
	}
	else {
		bacate.push(l);
	}

	if(bacate.includes(m)) {
		let fd = number();
		if(!bacate.includes(array[fd])) {
			bacate.push(array[fd]);
		}
	}
	else {
		bacate.push(m);
	}

	if(bacate.includes(n)) {
		let fd = number();
		if(!bacate.includes(array[fd])) {
			bacate.push(array[fd]);
		}
	}
	else {
		bacate.push(n);
	}

	if(bacate.includes(o)) {
		let fd = number();
		if(!bacate.includes(array[fd])) {
			bacate.push(array[fd]);
		}
	}
	else {
		bacate.push(o);
	}

	if(bacate.includes(p)) {
		let fd = number();
		if(!bacate.includes(array[fd])) {
			bacate.push(array[fd]);
		}
	}
	else {
		bacate.push(p);
	}

	if(bacate.includes(q)) {
		let fd = number();
		if(!bacate.includes(array[fd])) {
			bacate.push(array[fd]);
		}
	}
	else {
		bacate.push(q);
	}

	if(bacate.includes(r)) {
		let fd = number();
		if(!bacate.includes(array[fd])) {
			bacate.push(array[fd]);
		}
	}
	else {
		bacate.push(r);
	}

	if(bacate.includes(s)) {
		let fd = number();
		if(!bacate.includes(array[fd])) {
			bacate.push(array[fd]);
		}
	}
	else {
		bacate.push(s);
	}

	if(bacate.includes(t)) {
		let fd = number();
		if(!bacate.includes(array[fd])) {
			bacate.push(array[fd]);
		}
	}
	else {
		bacate.push(t);
	}

	return bacate.join('ㅤㅤ ');
}

module.exports = text;